# The design benchmark

The home page as pinned by Geoff on 2026-07-08 ("that's our new design benchmark"), at commit
9b0f415, captured from the deployed dev site with real photography and data. Every subsequent
page pass anchors against these renders — the section rhythm, the band and card grammar, the
list registers, the type scale in use, the link idiom (translucent underline, hover strengthen,
branded focus ring), the framed-image treatment, and the felt-refinement floor (audited 2026-07-08:
selection color, text-wrap, focus, cursor affordances all deliberate).

- `home-benchmark-1440.png` — desktop, full page
- `home-benchmark-390.png` — mobile, full page

A page pass is graded against these the way a port is graded against its original: the
benchmark is the reference render, not a verbal description of it. If the benchmark itself
evolves, re-pin deliberately (new captures, new commit reference, Geoff's sign-off) rather
than letting it drift.
